// Define o tempo decorrido em dias
const d = 0;
// Por que existe? NÃO SEI
// O que faz? NÃO SEI
// E como é usado? como uma constante númerica

// Nomes melhores
const tempoDecorridoEmDias = 0;
const elapseTimeInDays = 0;
// Por que existe? Para armazenar o tempo decorrido em dias
// O que faz? Armazena valor númerico que representa o tempo decorrido em dias
// E como é usado? como uma constante númerica

const diasDesdeACriacao = 0;
const daysSinceCreation = 0;
// Por que existe? Para armazenar o tempo decorrido desde a criação
// O que faz? Armazena valor númerico que representa o tempo decorrido desde a criação.
// E como é usado? como uma constante númerica

const diasDesdeAModification = 0;
const daysSinceModification = 0;
// Por que existe? Para armazenar os dias desde a modificação
// O que faz? Armazena um valor númerico que representa os dias desde a modificação
// E como é usado? como uma constante númerica


const idadeDoArquivoEmDias = 0;
const fileAgeInDays = 0;
// Por que existe? Para armazenar a idade do arquivo em dias
// O que faz? Armazena um valor númerico que representa a idade do arquivo em dias
// E como é usado? como uma constante númerica